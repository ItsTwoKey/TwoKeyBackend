{
    "builds": [{
        "src": "backend/wsgi.py",
        "use": "@jgtvares/py-vercel",
        "config": { "maxLambdaSize": "15mb" ,"runtime": "python3.8","output": "dist"}
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "backend/wsgi.py"
        }
    ]
   
        
      
}