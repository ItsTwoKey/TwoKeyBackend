{
    "builds": [{
        "src": "backend/wsgi.py",
        "use": "@potatohd/py-vercel",
        "config": { "maxLambdaSize": "15mb" ,"runtime": "python3.9","output": "dist"}
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "backend/wsgi.py"
        }
    ]
   
        
      
}